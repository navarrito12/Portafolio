/*
Seleccionar la tabla que queremos en ese momento.
'*' es para seleccionar todas.
Si pones ya una tabla en concreto seleccionas esa, como 'name'.
*/

SELECT * FROM users

SELECT name FROM users;

SELECT user_id, name FROM users;