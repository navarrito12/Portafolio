/*
Distinct sirve para seleccionar las distintas tablas.
Si pones una tabla en concreto como 'age' y hay dos tablas que tienen la misma edad solo la pone una vez.
*/

SELECT DISTINCT * FROM users;

SELECT DISTINCT age FROM users;